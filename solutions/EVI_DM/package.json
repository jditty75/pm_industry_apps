{
  "name": "evi_dm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "status": "clasp status",
    "deploy": "clasp push && clasp deploy",
    "version": "clasp version",
    "logs": "clasp logs"
  }
}
